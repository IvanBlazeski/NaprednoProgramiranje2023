// IVAN BLAZESKI INKI1006
#include <stdio.h>      
#include <time.h>       

int main ()
{
  time_t rawtime;
  struct tm * timeinfo;

  time ( &rawtime );
  timeinfo = localtime ( &rawtime );
  printf ( "The current date/time is: %s", asctime (timeinfo) );

  return 0;
}
/*Оваа програма C++ е слична на претходната, но користи понапредни функции од библиотеката time.h за форматирање на датумот и времето.

Програмата најпрво декларира две променливи: rawtime од типот time_t, кое го складира тековното време и timeinfo од типот struct tm, кое ќе се користи за форматирање на времето.

Функцијата time() потоа се повикува со покажувач на rawtime како нејзин аргумент, кој го враќа тековното време од системот и го складира во променливата rawtime.

Следно, функцијата localtime() се повикува со покажувач на rawtime како негов аргумент. Оваа функција зема променлива time_t како влез и враќа структура tm што го претставува локалното време. 

Добиената структура потоа се зачувува во променливата timeinfo.

Конечно, функцијата asctime() се повикува со структурата timeinfo како нејзин аргумент. 
Оваа функција зема променлива struct tm како влез и враќа низа што го претставува времето во формат читлив за луѓе. 
Добиената низа потоа се печати во конзолата користејќи ја функцијата printf().

Програмата потоа враќа 0 за да покаже успешно завршување.

Генерално, програмата го враќа тековното локално време и го форматира како стринг користејќи ги функциите localtime() и asctime() од библиотеката time.h,

и ја печати добиената низа во конзолата користејќи ја функцијата printf().*/
